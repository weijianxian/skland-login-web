[project]
name = "skland-login-web"
version = "1.0.0"
description = "森空岛自动签到 Web 服务"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "flask>=3.0",
    "apscheduler>=3.10,<4",
    "requests>=2.31",
    "cryptography>=41.0",
    "uvicorn>=0.30",
]
